Need help with grep ? 
grep -i: Ignore case (search for "Error" or "error").
grep -v: Invert match (show everything except the pattern).
grep -r: Recursive search (look through all files in a directory).
grep -w: Match whole words only (prevents "can" from matching "scan").
grep -l: List only the filenames, not the matching lines.
